# test env manifest to setup dependencies under <west topdir>
# should be initialized with cd repo && west init -l . --mf config/west.yml
manifest:
  self:
    path: config
    import: west-dependency.yml

